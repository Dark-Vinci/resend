import React, { JSX } from 'react';

import style from './mid.module.scss';

export function Mid(): JSX.Element {
    return (
        <div className={style.container}>
            Mid
        </div>
    );
}
